import { Hono } from 'hono'
// @ts-ignore generated by `ata build`: a standalone module that imports nothing
import { isValid } from './gen/body.mjs'
const app = new Hono()
app.post('/u', async (c) => {
  const body = await c.req.json()
  if (!isValid(body)) return c.json({ ok: false }, 400)
  return c.json({ ok: true, id: (body as any).id })
})
export default app
